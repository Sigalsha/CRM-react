import React from "react";
import "../../styles/actions/updateHeader.css";

const UpdateHeader = ({ text }) => {
  return <div className="update-header">{text}</div>;
};

export default UpdateHeader;
